[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
asyncio_mode = auto
addopts = -v --tb=short
markers =
    unit: unit tests (tools in isolation)
    integration: integration tests (agent + tools)
    eval: eval cases (query -> expected tool + output)
